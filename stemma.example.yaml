# yaml-language-server: $schema=https://raw.githubusercontent.com/woodleighschool/stemma/main/stemma.schema.json
apiVersion: stemma/v1alpha1
kind: Project
metadata:
  name: my-apps
spec:
  imports:
    - software/*.yaml
  destinations:
    munki:
      operation: munki
      config:
        path: .stemma/munki
